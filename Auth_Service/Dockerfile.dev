FROM maven:3.9.6-eclipse-temurin-21

WORKDIR /app

# Prime dependency cache for faster first startup in dev mode.
COPY pom.xml ./
RUN mvn -B -q -DskipTests dependency:go-offline

EXPOSE 8080

CMD ["mvn", "-Dspring-boot.run.fork=false", "spring-boot:run"]
